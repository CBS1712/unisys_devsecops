version: '3.9'
services: 
  cbs-springboot:
   image: cbsspring:tomcatv1
   build:
     context: . #location of dockerfile
     dockerfile: Dockerfile
   container_name: cbsspringc1
   ports:
   - 4980:5000

  #above steps have merging build and run